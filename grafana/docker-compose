services:
  grafana:
    image: grafana/grafana
    container_name: grafana
    restart: always
    ports:
      - 3000:3000
    networks:
      - monitoring
    volumes:
      - grafana-data:/vol01/Docker/monitoring
networks:
  monitoring:
volumes:
  grafana-volume:
    external: true